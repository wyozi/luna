local cats = {
	{ name = "Doge", age = 12 },
	{ name = "Dogger", age = 6 },
	{ name = "Pupper", age = 1 }
}
for i,{name, age} of cats do
	assert(name == cats[i].name)
	assert(age == cats[i].age)
end

local arr = { "hello", "world" }
for k,v of arr do
end
for i,v ofi arr do
end

local oo = nil
for x of? oo do
	assert(false, "nil collection iterated")
end